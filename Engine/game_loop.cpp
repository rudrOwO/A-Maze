#include <SFML/Graphics.hpp>
#include "../Levels/level_0.txt"

// Impossible until it is done

int main()
{
    // setting up the game window
    sf::RenderWindow window(sf::VideoMode::getDesktopMode(), "Rise!");
    window.setFramerateLimit(60);
    
    return 0;
}